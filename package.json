{
  "name": "mineorbuy",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev --workspace=@mineorbuy/web",
    "test": "npm run test --workspace=@mineorbuy/engine"
  }
}
